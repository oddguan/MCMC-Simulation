const assert = require('assert');
const mcmcSimulation = require('../index.js');

describe('mcmcSimulation', () => {
  it('has a test', () => {
    assert(false, 'mcmcSimulation should have a test');
  });
});
